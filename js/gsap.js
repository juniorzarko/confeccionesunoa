let tl =gsap.timeline ({
    repeat:0,
});
tl.from('.btn-1',{
    duration: 0.3,
    ease: "back.out(1",
    x:1200
});
tl.from('.btn-2',{
    duration: 0.3,
    ease: "back.out(1)",
    x:1200,
});

tl.from('.btn-3',{
    duration: 0.3,
    ease: "back.out(1)",
    x:1200
});

tl.from('.btn-4',{
    duration: 0.3,
    ease: "back.out(1)",
    x:1200
});
tl.from('.btn-5',{
    duration: 0.3,
    ease: "back.out(1)",
    x:1200,
});

tl.from('.btn-6',{
    duration: 0.3,
    ease: "back.out(1)",
    x:1200
});
tl.from('.btn-7',{
    duration: 0.3,
    ease: "back.out(1)",
    x:1200,
});

tl.from('.btn-8',{
    duration: 0.3,
    ease: "back.out(1)",
    x:1200,
});


gsap.from('.ima-1',{
    duration: 2.5,
    ease: "back.out(1.7)",
    x:-550,
   
});